[project]
name = "eddy-coherence-ml"
version = "0.1.0"
description = "ML model to predict eddy coherence"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cmocean>=4.0.3",
    "matplotlib>=3.10.1",
    "pyyaml>=6.0.2",
    "torch>=2.6.0",
]
